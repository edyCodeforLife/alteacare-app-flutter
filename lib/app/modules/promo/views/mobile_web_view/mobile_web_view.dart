// Flutter imports:
import 'package:flutter/material.dart';

class MobileWebPromoDetailPage extends StatelessWidget {
  const MobileWebPromoDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
